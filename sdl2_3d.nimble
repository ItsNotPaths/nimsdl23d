# Package
version     = "0.1.0"
author      = "Paths"
description = "Software 3D renderer — perspective-correct rasteriser over SDL2"
license     = "MIT"
srcDir      = "src"

# Dependencies
requires "nim   >= 2.0.0"
requires "sdl2  >= 2.0.4"
requires "vmath >= 2.0.0"
